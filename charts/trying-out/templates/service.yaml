apiVersion: v1
kind: Service
metadata:
  name: {{ include "trying-out.fullname" . }}
  labels:
    {{- include "trying-out.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "trying-out.selectorLabels" . | nindent 4 }}
