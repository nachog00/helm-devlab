apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "trying-out.fullname" . }}-test-connection"
  labels:
    {{- include "trying-out.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "trying-out.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
